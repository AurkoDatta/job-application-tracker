# Job Application Tracker

A full-stack, self-hosted Kanban-style job application tracker.

- **Frontend**: `frontend/` — React (Vite)
- **Backend**: `backend/` — Spring Boot REST API
- **Database**: MongoDB (local)

Setup instructions will be added here as each part of the project is built out (see `claude.md` for the full project spec and conventions).
