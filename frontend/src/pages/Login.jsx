/**
 * Login page placeholder. Task 7 builds out the real form + auth wiring;
 * this just confirms the route renders.
 */
function Login() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <div className="text-center">
        <h1 className="font-mono text-2xl font-semibold uppercase tracking-widest text-stamp">
          Login
        </h1>
        <p className="mt-2 text-sm text-slate">Placeholder page — Task 7 builds this out.</p>
      </div>
    </main>
  )
}

export default Login
